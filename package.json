{
  "name": "lesson-two-phonebook-api",
  "version": "1.0.0",
  "description": "A RESTful API for managing phonebook contacts",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../helsinki-exercise-phonebook && npm run build && cp -r dist ../phonebook-api-render ",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Ionut Raducu",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
